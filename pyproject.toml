[project]
name = "gnode"
version = "0.1.18"
description = "Group nodes into a clean card UI — a facade over your existing graph."
license = { text = "MIT" }
requires-python = ">=3.9"

[project.urls]
Repository = "https://github.com/spiritform/gnode"

[tool.comfy]
PublisherId = "spiritform"
DisplayName = "GNODE"
Icon = ""
